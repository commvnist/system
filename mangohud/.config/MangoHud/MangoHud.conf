## ~/.config/MangoHud/MangoHud.conf

legacy_layout=0
hud_compact
horizontal

# Prevent the background from stretching across the screen.
# Tune this if text gets clipped.
width=520

# Position / size
position=top-left
offset_x=12
offset_y=12
font_size=24
round_corners=6
background_alpha=0
alpha=1.0
text_outline

# Core GPU metrics
gpu_stats
gpu_power
gpu_temp
gpu_core_clock
gpu_mem_clock
vram
gpu_text=GPU

# Core CPU metrics
cpu_stats
cpu_power
cpu_temp
cpu_mhz
ram
cpu_text=CPU

# FPS basics
fps
frametime
fps_text=FPS

# Toggle HUD
toggle_hud=Shift_R+F12
